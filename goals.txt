Basic Search Functionality:
Allow users to search multiple programming-related sites like Stack Overflow and Reddit, etc, etc using a query provided via the command line.

    Example: oxi --language rust "ownership issues"

Multi-Word Query Handling:
Ensure the tool accepts multi-word queries seamlessly by requiring users to enclose them in quotes.

Language Filtering:
Include an optional --language flag to filter search results based on the programming language (e.g., oxi --language rust).

Site-Specific Searches:
Add site-specific searches (e.g., search exclusively on Stack Overflow or Reddit, etc etc).

Default Behavior:
Define clear default behavior when optional flags or arguments (like --language) are not provided.